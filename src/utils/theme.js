import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#7bed9f",
    },
    secondary: {
      main: "#ff6b81",
    },
    common: {
      drawer: "#747d8c",
    },
  },
});

export default theme;
